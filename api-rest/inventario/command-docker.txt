docker pull mariadb:jammy 
docker run --detach --name mariadb-jammy  --env MARIADB_USER=root --env MARIADB_PASSWORD=123456 --env MARIADB_ROOT_PASSWORD=123456  mariadb:jammy
docker start mariadb-jammy
docker stop mariadb-jammy 

docker exect -it mariadb-jammy -u root -p 

docker inspect (information docker ip)


problem --security-opt seccomp=unconfined
